.form-wrapper
  .main-form
    %h4 Account
    = simple_form_for @account do |f|
      = f.input :name
      = f.input :description
      .form-buttons
        = link_to 'Cancel', accounts_path, class: 'btn btn-primary back-button'
        = f.submit class: 'btn btn-primary'
